# Open Questions

- Should components be typed value records, or should the first slice use a small tagged component table?
- Should asset references be path strings in scene JSON or stable numeric handles resolved by the renderer?
- What is the smallest headless evidence that proves component lifecycle without inventing an editor?
